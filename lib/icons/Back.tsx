import { ArrowLeft } from "lucide-react-native"; // Assuming 'Send' is the icon name in lucide-react-native
import { iconWithClassName } from "./iconWithClassName";

iconWithClassName(ArrowLeft);
export { ArrowLeft as Back };
